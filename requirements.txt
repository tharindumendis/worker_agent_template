# Core LangChain + LangGraph
langchain>=0.3.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0

# MCP SDK (server + client)
mcp>=1.0.0
fastmcp>=0.4.0

# LangChain → MCP tool adapter
langchain-mcp-adapters>=0.1.0

# Config / utilities
pyyaml>=6.0
python-dotenv>=1.0.0
mcp_server_shell>=0.1.0
